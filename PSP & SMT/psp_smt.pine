// This Pine Script® code is subject to the terms of the Mozilla Public License 2.0 at https://mozilla.org/MPL/2.0/
// © Mayya
//@version=6

indicator("PSP & SMT Divergence [Mayya]", shorttitle="PSP & SMT [Mayya]", overlay=true, max_lines_count=500, max_labels_count=500, max_boxes_count=500, max_bars_back=1000)

// # ========================================================================= #
// # |   Constants & Style Helpers   |
// # ========================================================================= #

//#region Constants & Helpers
color_c_green       = #089981
color_c_red         = #f23645
color_transparent   = #ffffff00

f_line_style(string s) =>
    switch s
        "Solid"  => line.style_solid
        "Dashed" => line.style_dashed
        "Dotted" => line.style_dotted
        => line.style_solid

f_label_size(string s) =>
    switch s
        "Tiny"   => size.tiny
        "Small"  => size.small
        "Normal" => size.normal
        "Large"  => size.large
        => size.normal

f_short_name(string sym) =>
    arr = str.split(sym, ":")
    arr.get(arr.size() - 1)
//#endregion

// # ========================================================================= #
// # |   Inputs: Correlated Symbols (3 Slots Maksimal)   |
// # ========================================================================= #

//#region Correlated Symbols
g_sym         = "📊 Simbol Korelasi (Maks. 3 Pair)"
use_custom_tf = input.bool(false, "Gunakan Custom Timeframe?", group = g_sym, tooltip = "Jika aktif, data simbol korelasi diambil dari timeframe khusus. Jika mati, mengikuti timeframe chart saat ini.")
custom_tf     = input.timeframe("60", "Custom Timeframe", group = g_sym)
target_tf     = use_custom_tf ? custom_tf : timeframe.period

// Slot 1
enable_s1     = input.bool(true, "Aktif", inline = "s1", group = g_sym)
sym1          = input.symbol("CME_MINI:NQ1!", "Pair 1", inline = "s1", group = g_sym)
inv1          = input.bool(false, "Inverse", inline = "s1", group = g_sym, tooltip = "Balikkan pergerakan harga untuk aset yang berkorelasi negatif (seperti DXY)")

// Slot 2
enable_s2     = input.bool(true, "Aktif", inline = "s2", group = g_sym)
sym2          = input.symbol("CME_MINI:ES1!", "Pair 2", inline = "s2", group = g_sym)
inv2          = input.bool(false, "Inverse", inline = "s2", group = g_sym)

// Slot 3
enable_s3     = input.bool(false, "Aktif", inline = "s3", group = g_sym)
sym3          = input.symbol("CBOT_MINI:YM1!", "Pair 3", inline = "s3", group = g_sym)
inv3          = input.bool(false, "Inverse", inline = "s3", group = g_sym)

s1_name = f_short_name(sym1)
s2_name = f_short_name(sym2)
s3_name = f_short_name(sym3)
//#endregion

// # ========================================================================= #
// # |   Inputs: SMT Divergence Settings   |
// # ========================================================================= #

//#region SMT Settings
g_smt              = "⚡ SMT Divergence (Smart Money Technique)"
smt_show           = input.bool(true, "Aktifkan Garis SMT", group = g_smt)
smt_line_color     = input.color(color_c_red, "Warna Garis SMT", inline = "smt_l", group = g_smt)
smt_line_style_str = input.string("Solid", "Gaya Garis", options = ["Solid", "Dashed", "Dotted"], inline = "smt_l", group = g_smt)
smt_line_width     = input.int(2, "Lebar", minval = 1, maxval = 4, inline = "smt_l", group = g_smt)

smt_left           = input.int(4, "Swing Lookback (Kiri)", minval = 1, maxval = 20, inline = "smt_sw", group = g_smt, tooltip = "Jumlah bar di sebelah kiri untuk mendeteksi swing pivot")
smt_right          = input.int(1, "Konfirmasi (Kanan)", minval = 1, maxval = 10, inline = "smt_sw", group = g_smt, tooltip = "Jumlah bar di sebelah kanan untuk konfirmasi swing pivot")
smt_min_bars       = input.int(3, "Min. Jarak Bar", minval = 1, maxval = 20, inline = "smt_dist", group = g_smt, tooltip = "Jarak minimum candle antar kedua swing point agar tidak bertumpuk")
smt_max_bars       = input.int(60, "Maks. Jarak Bar", minval = 5, maxval = 200, inline = "smt_dist", group = g_smt, tooltip = "Jarak maksimum candle antar kedua swing point untuk relevansi struktur market")
smt_max_lines      = input.int(5, "Maksimum Garis", minval = 1, maxval = 20, group = g_smt)

smt_lbl_show       = input.bool(true, "Tampilkan Label SMT", inline = "smt_lbl", group = g_smt)
smt_lbl_size_str   = input.string("Normal", "Ukuran", options = ["Tiny", "Small", "Normal", "Large"], inline = "smt_lbl", group = g_smt)
smt_lbl_color      = input.color(color_c_red, "Warna Teks", inline = "smt_lbl_c", group = g_smt)
smt_lbl_bg         = input.color(color_transparent, "Background", inline = "smt_lbl_c", group = g_smt)

smt_line_style     = f_line_style(smt_line_style_str)
smt_lbl_size       = f_label_size(smt_lbl_size_str)
//#endregion

// # ========================================================================= #
// # |   Inputs: Price Symmetry Pivot (PSP) Settings   |
// # ========================================================================= #

//#region PSP Settings
g_psp              = "🕯️ Price Symmetry Pivot (PSP)"
psp_show           = input.bool(true, "Aktifkan Pewarnaan Candle PSP", group = g_psp)
psp_bull_color     = input.color(color_c_green, "Warna Bullish PSP", inline = "psp_col", group = g_psp)
psp_bear_color     = input.color(color_c_red,   "Warna Bearish PSP", inline = "psp_col", group = g_psp)
//#endregion


// # ========================================================================= #
// # |   Data Hub: Fetch Correlated Pairs (Optimized Bundling)   |
// # ========================================================================= #

//#region Data Fetching
[t_o1, t_h1, t_l1, t_c1] = request.security(enable_s1 ? sym1 : syminfo.tickerid, target_tf, [open, high, low, close], lookahead = barmerge.lookahead_off)
raw_o1 = enable_s1 ? t_o1 : float(na)
raw_h1 = enable_s1 ? t_h1 : float(na)
raw_l1 = enable_s1 ? t_l1 : float(na)
raw_c1 = enable_s1 ? t_c1 : float(na)

[t_o2, t_h2, t_l2, t_c2] = request.security(enable_s2 ? sym2 : syminfo.tickerid, target_tf, [open, high, low, close], lookahead = barmerge.lookahead_off)
raw_o2 = enable_s2 ? t_o2 : float(na)
raw_h2 = enable_s2 ? t_h2 : float(na)
raw_l2 = enable_s2 ? t_l2 : float(na)
raw_c2 = enable_s2 ? t_c2 : float(na)

[t_o3, t_h3, t_l3, t_c3] = request.security(enable_s3 ? sym3 : syminfo.tickerid, target_tf, [open, high, low, close], lookahead = barmerge.lookahead_off)
raw_o3 = enable_s3 ? t_o3 : float(na)
raw_h3 = enable_s3 ? t_h3 : float(na)
raw_l3 = enable_s3 ? t_l3 : float(na)
raw_c3 = enable_s3 ? t_c3 : float(na)

// Handle Inverse transformation
s1_O = inv1 ? -raw_c1 : raw_o1
s1_H = inv1 ? -raw_l1 : raw_h1
s1_L = inv1 ? -raw_h1 : raw_l1
s1_C = inv1 ? -raw_o1 : raw_c1

s2_O = inv2 ? -raw_c2 : raw_o2
s2_H = inv2 ? -raw_l2 : raw_h2
s2_L = inv2 ? -raw_h2 : raw_l2
s2_C = inv2 ? -raw_o2 : raw_c2

s3_O = inv3 ? -raw_c3 : raw_o3
s3_H = inv3 ? -raw_l3 : raw_h3
s3_L = inv3 ? -raw_h3 : raw_l3
s3_C = inv3 ? -raw_o3 : raw_c3
//#endregion

// # ========================================================================= #
// # |   PSP Engine: Candle Divergence Detection   |
// # ========================================================================= #

//#region PSP Logic
bool psp_bull_s1 = enable_s1 and not na(s1_C) and (close > open) and (s1_C < s1_O)
bool psp_bull_s2 = enable_s2 and not na(s2_C) and (close > open) and (s2_C < s2_O)
bool psp_bull_s3 = enable_s3 and not na(s3_C) and (close > open) and (s3_C < s3_O)
bool is_psp_bull = psp_bull_s1 or psp_bull_s2 or psp_bull_s3

bool psp_bear_s1 = enable_s1 and not na(s1_C) and (close < open) and (s1_C > s1_O)
bool psp_bear_s2 = enable_s2 and not na(s2_C) and (close < open) and (s2_C > s2_O)
bool psp_bear_s3 = enable_s3 and not na(s3_C) and (close < open) and (s3_C > s3_O)
bool is_psp_bear = psp_bear_s1 or psp_bear_s2 or psp_bear_s3

// Terapkan Pewarnaan Candle PSP (Default: Hijau Bullish, Merah Bearish)
color psp_color = na
if psp_show
    if is_psp_bull
        psp_color := psp_bull_color
    else if is_psp_bear
        psp_color := psp_bear_color

barcolor(psp_color, title = "Warna Candle PSP")
//#endregion

// # ========================================================================= #
// # |   SMT Engine: Precision Structural Swing Divergence & Direct Pair Label |
// # ========================================================================= #

//#region SMT Logic
// Deteksi Structural Pivot menggunakan ta.pivotlow dan ta.pivothigh
float pl_pivot = ta.pivotlow(low, smt_left, smt_right)
float ph_pivot = ta.pivothigh(high, smt_left, smt_right)

bool is_swing_low  = not na(pl_pivot)
bool is_swing_high = not na(ph_pivot)

// Offset bar terjadinya pivot
int p_offset = smt_right

// Variabel memori untuk swing low sebelumnya
var float prev_pl_price  = na
var int   prev_pl_time   = na
var int   prev_pl_bar    = na
var float prev_pl_s1_low = na
var float prev_pl_s2_low = na
var float prev_pl_s3_low = na

// Variabel memori untuk swing high sebelumnya
var float prev_ph_price   = na
var int   prev_ph_time    = na
var int   prev_ph_bar     = na
var float prev_ph_s1_high = na
var float prev_ph_s2_high = na
var float prev_ph_s3_high = na

// Array storage untuk visual garis & label
var line[]  smt_lines_arr  = array.new_line()
var label[] smt_labels_arr = array.new_label()

// ── Evaluasi SMT Bullish (Swing Low) ──────────────────────────
bool is_smt_bull = false
string bull_pair_txt = ""

if is_swing_low and smt_show
    float curr_pl_price  = low[p_offset]
    int   curr_pl_time   = time[p_offset]
    int   curr_pl_bar    = bar_index[p_offset]
    float curr_s1_low    = s1_L[p_offset]
    float curr_s2_low    = s2_L[p_offset]
    float curr_s3_low    = s3_L[p_offset]
    
    // Periksa apakah swing low sebelumnya ada dan jaraknya valid
    if not na(prev_pl_price)
        int bar_distance = curr_pl_bar - prev_pl_bar
        if bar_distance >= smt_min_bars and bar_distance <= smt_max_bars
            bool s1_div = false
            bool s2_div = false
            bool s3_div = false
            
            // Slot 1
            if enable_s1 and not na(prev_pl_s1_low) and not na(curr_s1_low)
                if (curr_pl_price < prev_pl_price and curr_s1_low > prev_pl_s1_low) or (curr_pl_price > prev_pl_price and curr_s1_low < prev_pl_s1_low)
                    s1_div := true
            
            // Slot 2
            if enable_s2 and not na(prev_pl_s2_low) and not na(curr_s2_low)
                if (curr_pl_price < prev_pl_price and curr_s2_low > prev_pl_s2_low) or (curr_pl_price > prev_pl_price and curr_s2_low < prev_pl_s2_low)
                    s2_div := true
            
            // Slot 3
            if enable_s3 and not na(prev_pl_s3_low) and not na(curr_s3_low)
                if (curr_pl_price < prev_pl_price and curr_s3_low > prev_pl_s3_low) or (curr_pl_price > prev_pl_price and curr_s3_low < prev_pl_s3_low)
                    s3_div := true
            
            if s1_div or s2_div or s3_div
                is_smt_bull := true
                
                // Rangkai nama korelasi murni (langsung nama pair korelasi saja!)
                if s1_div
                    bull_pair_txt := s1_name
                if s2_div
                    bull_pair_txt := bull_pair_txt == "" ? s2_name : bull_pair_txt + " & " + s2_name
                if s3_div
                    bull_pair_txt := bull_pair_txt == "" ? s3_name : bull_pair_txt + " & " + s3_name
                
                // Buat garis SMT tepat di antara dua titik swing low
                line l_smt = line.new(x1 = prev_pl_time, y1 = prev_pl_price,
                                      x2 = curr_pl_time, y2 = curr_pl_price,
                                      xloc = xloc.bar_time, color = smt_line_color,
                                      width = smt_line_width, style = smt_line_style)
                
                // Label tepat di tengah garis dengan teks langsung nama pair korelasi
                int   mid_x = int(math.avg(prev_pl_time, curr_pl_time))
                float mid_y = math.avg(prev_pl_price, curr_pl_price)
                
                label l_lbl = smt_lbl_show ? label.new(x = mid_x, y = mid_y, text = bull_pair_txt,
                                                        xloc = xloc.bar_time, color = smt_lbl_bg,
                                                        textcolor = smt_lbl_color, style = label.style_label_center,
                                                        size = smt_lbl_size) : na
                
                array.push(smt_lines_arr, l_smt)
                if not na(l_lbl)
                    array.push(smt_labels_arr, l_lbl)
                
                while array.size(smt_lines_arr) > smt_max_lines
                    line.delete(array.shift(smt_lines_arr))
                while array.size(smt_labels_arr) > smt_max_lines
                    label.delete(array.shift(smt_labels_arr))
    
    // Perbarui state swing low
    prev_pl_price  := curr_pl_price
    prev_pl_time   := curr_pl_time
    prev_pl_bar    := curr_pl_bar
    prev_pl_s1_low := curr_s1_low
    prev_pl_s2_low := curr_s2_low
    prev_pl_s3_low := curr_s3_low


// ── Evaluasi SMT Bearish (Swing High) ─────────────────────────
bool is_smt_bear = false
string bear_pair_txt = ""

if is_swing_high and smt_show
    float curr_ph_price   = high[p_offset]
    int   curr_ph_time    = time[p_offset]
    int   curr_ph_bar     = bar_index[p_offset]
    float curr_s1_high    = s1_H[p_offset]
    float curr_s2_high    = s2_H[p_offset]
    float curr_s3_high    = s3_H[p_offset]
    
    if not na(prev_ph_price)
        int bar_distance = curr_ph_bar - prev_ph_bar
        if bar_distance >= smt_min_bars and bar_distance <= smt_max_bars
            bool s1_div = false
            bool s2_div = false
            bool s3_div = false
            
            // Slot 1
            if enable_s1 and not na(prev_ph_s1_high) and not na(curr_s1_high)
                if (curr_ph_price > prev_ph_price and curr_s1_high < prev_ph_s1_high) or (curr_ph_price < prev_ph_price and curr_s1_high > prev_ph_s1_high)
                    s1_div := true
            
            // Slot 2
            if enable_s2 and not na(prev_ph_s2_high) and not na(curr_s2_high)
                if (curr_ph_price > prev_ph_price and curr_s2_high < prev_ph_s2_high) or (curr_ph_price < prev_ph_price and curr_s2_high > prev_ph_s2_high)
                    s2_div := true
            
            // Slot 3
            if enable_s3 and not na(prev_ph_s3_high) and not na(curr_s3_high)
                if (curr_ph_price > prev_ph_price and curr_s3_high < prev_ph_s3_high) or (curr_ph_price < prev_ph_price and curr_s3_high > prev_ph_s3_high)
                    s3_div := true
            
            if s1_div or s2_div or s3_div
                is_smt_bear := true
                
                if s1_div
                    bear_pair_txt := s1_name
                if s2_div
                    bear_pair_txt := bear_pair_txt == "" ? s2_name : bear_pair_txt + " & " + s2_name
                if s3_div
                    bear_pair_txt := bear_pair_txt == "" ? s3_name : bear_pair_txt + " & " + s3_name
                
                line l_smt = line.new(x1 = prev_ph_time, y1 = prev_ph_price,
                                      x2 = curr_ph_time, y2 = curr_ph_price,
                                      xloc = xloc.bar_time, color = smt_line_color,
                                      width = smt_line_width, style = smt_line_style)
                
                int   mid_x = int(math.avg(prev_ph_time, curr_ph_time))
                float mid_y = math.avg(prev_ph_price, curr_ph_price)
                
                label l_lbl = smt_lbl_show ? label.new(x = mid_x, y = mid_y, text = bear_pair_txt,
                                                        xloc = xloc.bar_time, color = smt_lbl_bg,
                                                        textcolor = smt_lbl_color, style = label.style_label_center,
                                                        size = smt_lbl_size) : na
                
                array.push(smt_lines_arr, l_smt)
                if not na(l_lbl)
                    array.push(smt_labels_arr, l_lbl)
                
                while array.size(smt_lines_arr) > smt_max_lines
                    line.delete(array.shift(smt_lines_arr))
                while array.size(smt_labels_arr) > smt_max_lines
                    label.delete(array.shift(smt_labels_arr))
    
    prev_ph_price   := curr_ph_price
    prev_ph_time    := curr_ph_time
    prev_ph_bar     := curr_ph_bar
    prev_ph_s1_high := curr_s1_high
    prev_ph_s2_high := curr_s2_high
    prev_ph_s3_high := curr_s3_high
//#endregion


// # ========================================================================= #
// # |   Alerts   |
// # ========================================================================= #

//#region Alerts
alertcondition(is_smt_bull, "Bullish SMT Divergence", "🟢 Bullish SMT Divergence terdeteksi pada {{ticker}}!")
alertcondition(is_smt_bear, "Bearish SMT Divergence", "🔴 Bearish SMT Divergence terdeteksi pada {{ticker}}!")
alertcondition(is_psp_bull, "Bullish PSP Candle",     "🟢 Bullish PSP Candle terdeteksi pada {{ticker}}!")
alertcondition(is_psp_bear, "Bearish PSP Candle",     "🔴 Bearish PSP Candle terdeteksi pada {{ticker}}!")
//#endregion